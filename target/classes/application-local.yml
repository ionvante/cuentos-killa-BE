spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cuentoskilla
    username: postgres
    password: 993753
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 20000
      idle-timeout: 300000
      max-lifetime: 1200000
      auto-commit: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        jdbc:
          fetch_size: 50
          batch_size: 10
        order_inserts: true
        order_updates: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${SPRING_MAIL_USERNAME:cuentoskilla@gmail.com}
    password: ${SPRING_MAIL_PASSWORD:nfug jaiv ythi jfbe}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

file:
  upload-dir: ./uploads/vouchers

boleta:
  upload-dir: ./uploads/boletas

storage:
  provider: local

firebase:
  credentials: ${FIREBASE_CREDENTIALS:firebase-service-account.json}
  bucket: ${FIREBASE_BUCKET:cuentoskilla-f84ff.firebasestorage.app}

mercadopago:
  access-token: YOUR_ACCESS_TOKEN # Existing
  back-urls:
    success: https://your-frontend.com/payment/success
    failure: https://your-frontend.com/payment/failure
    pending: https://your-frontend.com/payment/pending
  notification-url: https://your-api-backend.com/api/webhooks/mercadopago

jwt:
  secret: ${JWT_SECRET:local-secret}

frontend:
  url: http://localhost:4200



